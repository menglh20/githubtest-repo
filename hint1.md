# to use git to commit in the first way

+ git clone git@github.com:menglh20/xxx.git
+ git cd xxx
+ git add xxx
+ git commit -m "to write something"
+ git log
+ git status
+ git push origin master
  
### remember do not need to use git init