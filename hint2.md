## the second way to use git push

+ git init
+ git remote add origin git@github.com:menglh20/xxx.git
+ git pull origin master
+ git add xxx.cpp
+ git commit -m "to write something"
+ git push origin master